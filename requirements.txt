# Core dependencies
fastapi==0.116.1
uvicorn==0.35.0
starlette==0.47.3
pydantic==2.11.9
python-dotenv==1.1.1
orjson==3.11.9

# HTTP & Parsing
requests==2.32.4
beautifulsoup4==4.13.4
lxml==6.0.0

# Image processing & OCR
# ddddocr: 算式验证码整图识别(数字/运算符)。自训 NCNN ResNet 换字体后失效且无法重训，已弃用。
# 注意: 清华 tuna 镜像可能不收录 ddddocr; 镜像装失败时改用官方源:
#   pip install ddddocr -i https://pypi.org/simple
pillow==11.3.0
opencv-python==4.12.0.88
numpy==2.2.6
ddddocr>=1.4.7

# Utilities
tqdm==4.67.1
pycryptodome>=3.20.0  # captchaCode AES-128-CBC 加密(复现 SPA w()), 也兼容 cryptography 作回退

# Testing
pytest>=7.0
pytest-asyncio>=0.21
pytest-cov>=4.0
httpx>=0.24
