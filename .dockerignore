# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Git
.git/
.gitignore

# Docker
Dockerfile
docker-compose.yml

# 测试和文档
tests/
docs/
.claude/
scripts/
# README.md 是构建必需（pyproject readme 引用），只排除无关 md/文档
CLAUDE.md
Documents/
src/smu_badminton/docs/

# CI 与会话
.github/
.zcode/

# 调试和临时文件
*.log
*.tmp
*.har
captcha.png

# 真实配置与数据：绝不允许进镜像层
# （当前 Dockerfile 逐目录 COPY 不会带上它们，这里是为了防止将来改成 COPY . . 时
#   把 .env 里的地址/凭据与 data/ 下的库、密钥一起打进镜像）
.env
.env.*
!.env.example
data/

# Windows / macOS
Thumbs.db
Desktop.ini
.DS_Store
